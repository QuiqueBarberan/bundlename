name: 'Bundle name'
description: 'Returns the name of the release'
outputs:
  BUNDLE:
    description: "Bundle"
    value: ${{ steps.bundlestep.outputs.BUNDLE }}
runs:
  using: "composite"
  steps:
    - id: bundlestep
      run: echo "bundle=$(./scripts/bundle.sh)" >> $GITHUB_OUTPUT
      shell: bash
